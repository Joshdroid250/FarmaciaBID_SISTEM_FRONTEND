
@model FarmaciaBID.Models.Measures

@{
    ViewBag.Title = "Editar Medidas";
    Layout = null;
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <link href="~/Content/bootstrap-datepicker.min.css" rel="stylesheet" />

    <div class="form-horizontal m-4">
        <h4>Editar Medida</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
    <div class="row gx-3 mb-3">

        <div class="col-md-6 d-none invisible">
            <div class="input-group">
                <span class="input-group-text"><i class="fa fa-calendar" aria-hidden="true"></i></span>
                @Html.EditorFor(model => model.idMedidas, new { htmlAttributes = new { @class = "form-control", placeholder = "ID" } })
                @Html.ValidationMessageFor(model => model.idMedidas, "", new { @class = "text-danger" })
            </div>
        </div>
        <div class="col-md-6">
            <div class="input-group">
                <span class="input-group-text"><i class="fa fa-calendar" aria-hidden="true"></i></span>
                @Html.EditorFor(model => model.nombre, new { htmlAttributes = new { @class = "form-control", placeholder = "Nombre", required = "required" } })
                @Html.ValidationMessageFor(model => model.nombre, "", new { @class = "text-danger" })
            </div>
        </div>

        <!-- Agregar más campos según sea necesario -->
    </div>

        <hr />
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <button type="submit" value="Create" class="btn btn-primary" id="guardarBtn"><i class="bi-floppy" aria-hidden="true"></i> Guardar</button>

            </div>
        </div>
        <hr />
    </div>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    <script src="~/Scripts/bootstrap-datepicker.min.js"></script>

    <script>
        $(document).ready(function () {
            $('.datepicker').datepicker({
                format: 'yyyy-mm-dd',
                autoclose: true,
                endDate: new Date()
            });
        });
    </script>
}


